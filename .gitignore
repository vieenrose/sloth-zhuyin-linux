# External LLM runtime (cloned by scripts/setup-llm.sh, not vendored)
llm/llama.cpp/
llm/models/

# Build directories
build/
build*/

# eval harness build artifact
eval/harvest

# fine-tuning corpus / outputs (bring your own; large)
finetune/*.jsonl
finetune/out/
finetune/corpus*.txt
llm/venv/

# packaging build artifacts
*.deb
space-static/enc/model_quantized.onnx
model/slothe_4m_onnx/
model/slothe_10m_onnx/
model/slothe_10m_v2.pt

# --- 25M ternary deploy (libslothe) ---
# served/staged GGUF is fetched, not tracked (packaging/fetch-model.sh)
model/slothe_t_25m/
space-static/enc/slothe-t-25m.gguf
# HF publish staging: large weights live on HF; scripts/docs ARE tracked
model/hf_release/*.pt
model/hf_release/*.safetensors
model/hf_release/*.gguf
model/hf_release/slothe_tensors.npz
model/hf_release/golden/
# Android prebuilt ggml static libs — regenerate via android/build-ggml.sh
android/app/ggml/lib/
# daemon build dir
engine/slothd/build_slothe/
