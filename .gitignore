# External LLM runtime (cloned by scripts/setup-llm.sh, not vendored)
llm/llama.cpp/
llm/models/

# Build directories
build/
build*/

# eval harness build artifact
eval/harvest

# fine-tuning corpus / outputs (bring your own; large)
finetune/*.jsonl
finetune/out/
finetune/corpus*.txt
llm/venv/

# packaging build artifacts
*.deb
space-static/enc/model_quantized.onnx
model/slothe_4m_onnx/
